# Ashan App (业务扩展 ashan_cn_procurement) 核心模块设计理念与 UI/UX 规范手册

> **📌 本文件是本项目 (ERPNext 16 / ashan_cn_procurement) 的核心产品架构、模块业务设计哲学与 UI 风格记忆文件。**
> 任何新开启的对话或接手开发的 AI 助手，在进行业务扩展、页面设计、字段定义与 Excel 导出开发时，**必须严格遵守本手册中的设计理念与规范**。

---

## 🌟 一、全局 UI/UX 风格与交互设计哲学

本项目致力于打造**极简、精致、高信息密度、原生优先且 1:1 贴合中国企业真实业务习惯**的企业级 ERPNext 16 扩展体验。

### 1. 核心设计原则
1. **原生优先 (Frappe Native First)**：
   - 优先使用 Desk Page、Form、List、Report 等 Frappe 原生能力；
   - 杜绝为了“看起来像 SaaS”而引入笨重的前端框架或过度设计的复杂卡片；
   - 业务校验与核心计算必须放在服务器端，前端负责即时交互与数据渲染。
2. **极简低噪 (Low Visual Noise & Function-Driven)**：
   - 拒绝大面积无意义渐变、彩色外发光、超大字号和无意义的卡片堆叠；
   - 表格优先于卡片流，数据与依据一目了然，方便财务、行政、运维人员快速核对。
3. **严格统一的控制栏尺寸 (36px Uniform Control Bar)**：
   - 工作台（Workbench）和页面顶部控制栏中的所有组件（如【年月选择器】、【输入框】、【操作按钮】）高度**严格统一为 `36px`**，水平基线完全对齐，精致稳重。
4. **【4个以内选项原则 (Segmented Tabs > Dropdown)】**：
   - 当表单、弹窗或筛选器中字段的备选项在 **4 个以内（含 2~4 个选项）** 时，**坚决禁止**使用默认折叠的下拉菜单（`<select>`）；
   - **必须优先采用单次点击即生效的分段选项卡 / 胶囊单选卡片组（Segmented Tabs / Pill Toggle Chips）**；
   - **人机工效依据**：下拉菜单需要“展开+寻址+点选”共 2 次点击且隐藏上下文；分段选项卡全平铺，**1 次点击即达**，直观高效，大幅降低认知负荷与操作摩擦力。
5. **防拦截与原生交互 (Robust Native Interaction)**：
   - 导出 Excel 与报表下载采用原生动态创建链接触发（`<a>` download），坚决杜绝浏览器弹窗拦截。
6. **大宽表人体工学视口常驻与列冻结铁律 (Ergonomic Wide Table Viewport & Column Freezing)**：
   - **横向滚动条视口常驻**：表格容器 (`.qifu-table-box`) 必须设置 `max-height: calc(100vh - 280px); overflow: auto;`，横向滑轮直接显露在屏幕可视区底部，**严禁**让财务人员拉到网页最底部才能向右滑动！
   - **多层表头分层吸顶 (`position: sticky; top: 0 / 30px`)**：向下滚动流水时，表头分组与列名始终锁定在顶部。
   - **左侧核心身份列冻结 (`position: sticky; left: 0 / 45px / 110px`)**：向右滑动查看各险金明细、税阶、实发等长宽数据时，`序号`、`工号`、`姓名` 始终固定在左侧（附带阴影分界线），保持员工身份锚定。


---

## 🧭 二、企业级侧边栏 (Sidebar) 导航体系标准

系统全局侧边栏已确立**“职责分离”双区交互模型 (Dual-Zone Action Model)**：

```text
├── 🏢 我的业务 (总控主页) [/desk/my-business]
├── 🏢 物业与租赁 (Section Break, 展开/折叠兼跳转对应 Dashboard)
│   ├── 物业月结工作台 [/desk/property-settlement-workbench]
│   ├── 租赁物业档案 [/desk/property-lease]
│   ├── 收费标准版本 [/desk/property-charge-rate]
│   ├── 水电表配置 [/desk/utility-meter]
│   └── 月结历史总账 [/desk/property-monthly-settlement]
├── 🚗 车辆和车用油管理
│   ├── 车辆档案 / 油卡档案
│   ├── 油卡综合台账明细台 [/desk/fuel-ledger-workbench]
│   ├── 高速费月度台账
│   └── 加油与能耗记录 / 油票批量录入
└── 🛡️ 企业合规中心
    ├── 环保管理工作台 / 特种设备管理工作台
    ├── 特种设备档案 / 法定检验记录 / 年度检查记录
    └── 环保事项台账 / 员工证书资质
```

### 侧边栏视觉与行为铁律：
- **一级标题 (Section Break)**：加粗展示（`font-weight: 700; font-size: 13.5px;`），配备专属功能图标与折叠小箭头。点击文字跳转对应分类大屏并折叠/展开；点击右侧独立箭头纯折叠/展开。
- **二级菜单 (Sub-items)**：**严格禁止图标**，纯文字左缩进 24px（`font-size: 13px; font-weight: 400;`），纯粹进行 SPA 路由切换，**绝对不触发任何折叠**。
- **全局根路径拦截**：系统根路径 `/`、`/app`、`/desk` 统一默认进入总控主页 `/desk/my-business`。

---

## 📦 三、各业务模块设计理念与落地逻辑

### 🏢 模块 1: 物业与租赁管理 (Property & Lease Management)

#### 1. 业务痛点与设计定位
工厂与园区租赁中，往往包含多家关联公司（如**天津吉众机电设备有限公司**、**天津祺富机械加工有限公司**），涉及**按平米日/月/年多周期自选计费**、**房租是否含物业费**、**工业电表高压倍率（CT变比如 ×120, ×80, ×40）**、**公司间电费分摊调整**以及**1:1 精准复刻标准 Excel 单证**。

#### 2. 核心 DocType 架构
- `Property Lease` (租赁物业档案)：记录场地名称、归属公司、租赁面积（㎡）、物业费计收模式（房租含物业 / 单独计收物业费）。
- `Property Charge Rate` (收费标准版本)：支持多周期自动换算：
  - 房租单价支持：按年总金额（元/年）、按月总金额（元/月）、按日单价（元/㎡·天）、按月单价（元/㎡·月）、按年单价（元/㎡·年）；
  - 单独物业费支持：按年总金额、按月总金额、按日单价、按月单价、按年单价；
  - 严格按国家标准 **365 天** 进行日租金折算与当月天数（30/31天）核算。
- `Utility Meter` (水电表配置)：表号、倍率（Multiplier，如 120）、初始表底数、归属公司、电/水类别。
- `Property Monthly Settlement` (月结总账) & `Property Settlement Workbench` (月结单页工作台)：
  - **抄表矩阵**：自动带出上期底数，输入本期读数，自动计算 `(本期-上期) × 倍率 × 单价`。
  - **费用调整**：支持按金额/按用量，支持单公司修正或公司间分摊转移（如吉众转出 8000 元至祺富）。
  - **调整项展示简化**：在工作台及单据中统一简洁展示为 **`电费调整`** 或 **`水费调整`**。

#### 3. Excel 导出 1:1 标准单证设计哲学 (Split-Sheet Architecture)
导出的 Excel 完全依照企业标准单证，具有以下绝对规范：
1. **Sheet 拆分原则**：
   - 导出单公司时拆分为 **2 个 Sheet**：`{公司短名}水电费`（如 `吉众水电费`）和 `{公司短名}房租物业`（如 `吉众房租物业`）。
   - 导出合计单证时拆分为 **2 个 Sheet**：`合计水电费` 和 `合计房租物业`。
   - 导出全套工作簿时包含所有分公司的水电、房租 Sheet 及合计 Sheet（共 6 个 Sheet）。
2. **表头第 3 行统一为「所属期」**：
   - 第 1 行：公司抬头（等线 18pt 粗体居中）
   - 第 2 行：副标题（`水电费明细（单价含税）` 或 `房租物业费明细（单价含税）`）
   - 第 3 行：所属期合并行：`所属期: YYYY-MM    物业公司: 天津金利达物业管理有限公司`
3. **水电费 Sheet 彻底剥离房租**：
   - 仅包含电费表、水费表、调整项以及底部的【水电费合计汇总表】；
   - 表头仅保留【表号】，去除表具名称；
   - 汇总表右侧大字合并单元格显示 `水电费合计`（等线 20pt 居中）。
4. **房租物业 Sheet 独立展示年缴费与当期折算**：
   - 包含【场地名称】、【年金额】、【面积(㎡)】、【计费天数】、【日单价】、【物业费计收方式】、【所属期金额】；
   - 底部独立核算房租、物业费及含税合计。

---

### 🚗 模块 2: 车辆与车用油管理 (Vehicle & Fuel Management)

#### 1. 业务定位
解决企业公务车、卡车、油卡充值与各车辆实际加油流水之间的“充值-分配-消费-发票抵扣”全链路核算。
#### 2. 核心架构与功能
- `Vehicle Profile` (车辆档案)：车牌号、车型、责任司机、油品型号、百公里基准油耗。
- `Oil Card Profile` (油卡档案)：油卡卡号、发卡机构（中石化/中石油）、卡余额、绑定车辆/主副卡体系。
- `Oil Card Recharge` (油卡充值流水)：记录母卡充值、圈存明细与银行打款凭证。
- `Fuel Entry` (加油与能耗记录)：记录每次加油升数、单价、金额、当前里程表读数、自动计算百公里油耗与异常预警。
- `Fuel Invoice Batch` (油票批量录入) & `Highway Toll Ledger` (高速费台账)：发票快速匹配与月度车辆综合能耗成本看板。

---

### 🛡️ 模块 3: 企业合规中心 (Corporate Compliance Center)

#### 1. 业务定位
针对工厂企业的安全生产、特种设备法定检验周期、环保合规与员工资质证书到期预警。
#### 2. 核心架构与功能
- `Special Equipment Profile` (特种设备档案)：起重机械、叉车、压力容器、压力管道等设备铭牌、编号、使用登记证。
- `Statutory Inspection Record` (法定检验记录) & `Annual Check Record` (年度检查记录)：记录年检周期、到期日预警灯（绿/黄/红）。
- `Environmental Compliance Record` (环保事项台账)：排污许可、危废转移联单、废气废水监测报告台账。
- `Employee Qualification Certificate` (员工证书资质)：特种作业操作证（焊工、电工、叉车司机）、安全员证书及复审到期预警。

---

### 💰 模块 4: 中国税控发票与采购财务集成 (China Tax & Procurement)

#### 1. 规格型号统一铁律
- **规格型号严禁创建自定义字段**：物料、采购订单、采购收货单、采购发票等统一使用原生 `description` 字段（单行文本 Data 类型），界面标签显示为 **“规格”**。
- 严禁出现 `custom_guige_xinghao` 或 `custom_spec_model` 等重复字段。
#### 2. 采购发票 (Purchase Invoice) 中国税控联动
- 原生集成中国增值税税率（13%、9%、6%、3%、0%）；
- 支持含税单价/金额与无税单价/金额的双向联动计算，税额自动拆分，严格符合中国金税系统尾差修约规则；
- AI 发票匹配推荐仅作为辅助依据，最终必须通过 ERPNext 标准 Controller 与权限校验完成入账。

---

### 🏛️ 模块 5: 税局发票资料库与数电发票智能核验 (Tax Invoice Center)

#### 1. 业务定位
解决中国税务数电发票平台导出压缩包（ZIP/XML/PDF）的**独立归档、多抬头智能识别、免录入红冲对冲与 ERP 采购发票自动核对**。
#### 2. 核心架构与功能
- `Tax Invoice` (税局发票独立档案)：记录发票号码、开票日期、购买方全称、销售方全称、不含税/税额/车船税/应付合计、结构化明细子表及 PDF 私有附件。
- `Tax Invoice Import Batch` (导入批次)：支持 Windows GBK / UTF-8 混合编码 ZIP 智能解压，数电 20 位发票号码提取配对。
- **购买方多抬头智能归属引擎 (`identify_company`)**：
  - 核心商号识别：包含“吉众”自动归属于 `天津吉众机电设备有限公司`；包含“祺富”自动归属于 `天津祺富机械加工有限公司`；
  - 持久化别名表：`Tax Invoice Settings` 预置映射，彻底消除多主体归属混淆。
- **红字发票与原蓝字发票自动红冲对冲闭环 (`auto_reconcile_red_invoices`)**：
  - **Level 1 (官方精准关联)**：依据红字发票中的 `original_invoice_no` 查找原蓝字发票；
  - **Level 2 (业务特征对冲)**：依据【销售方 + 购买方 + 金额相反 + 开票品名】智能特征配对；
  - **业务状态流转**：双方均未录入 ERP 时，自动标记为 **【已对冲 (无需录入 ERP)】**，互相建立溯源链接并移出【待录入】待办；若原蓝字已入账 ERP，红字保持【待录入】提示财务在 ERP 录入红字采购发票冲账。
- **5 项 KPI 看板**：待录入发票 (⏳)、已录入 ERP (✅)、已红冲对冲(无需录入) (🔄)、已废弃 (🗑️)、需复核 (⚠️)。

---

### 🍱 模块 6: 订餐与工作餐费用核定 (Meal Settlement Workbench)

#### 1. 业务定位
吉众与祺富每月工作餐订餐明细（Excel 上传与手动录入混合模式）智能核定、多主体分摊与费用月结。
#### 2. 核心架构与功能
- `Meal Settlement Workbench`：支持 Excel 订餐记录一键拖拽解析与日明细快速补录。
- **单价保存与全月联动**：基准单价（如 15 元/份）配备【💾 保存并应用】按钮，一键批量重算全月所有日明细并持久化。
- **操作与防呆**：配备【🗑️ 清空本月】重置功能（二次确认）与 `Ctrl + S` 快捷保存。
- **权限模型**：同一操作人员只需拥有吉众或祺富任一权限即可统筹处理双主体餐费。

---

### 🎨 模块 7: 全局企业级交互组件库 (`AshanUI Kit`)

为彻底消除由于文本长短切换导致的按钮与文字左右抖动、风格不一致等问题，全局注入 `AshanUI Kit`：
- **稳态自动保存胶囊 (`AshanUI.createSaveIndicator`)**：固定 140px 容器，状态切换零位移、零抖动。
- **统一年月导航与卡片组件**：提供统一的 `AshanUI.createMonthSelector` 与标准卡片容器。
- **标准化金额与徽章规范**：统一金额格式为 `¥ XXX,XXX.XX` 或 `XXXX元`，统一状态徽章色彩语义。

---

## 🏷️ 四、跨单据通用字段与 UI 呈现规范 (Universal Field & UI Consistency)

为了保证全系统在多单据流转过程中的**极致统一性、高信息密度与极佳用户体验**，凡跨多单据使用的业务字段，必须严格遵守以下铁律：

### 1. 核心铁律：相同语义必须高度复用与统一
- **单据明细统一命名**：凡用于汇总子表品名、数量、金额等摘要信息的字段，统一命名为 **`custom_doc_details`**，界面标签统一为 **【单据明细】**。
- **只读与自动生成**：字段设为只读 (`read_only: 1`)，统一通过后端引擎 `ashan_cn_procurement/overrides/document_details.py` 在单据 `validate` 时自动推导生成，禁止前端手工录入。
- **覆盖单据范围**：
  - 采购申请 (`Material Request`)
  - 采购订单 (`Purchase Order`)
  - 物资入库 (`Purchase Receipt`)
  - 采购发票 (`Purchase Invoice`)
  - 报销申请 (`Reimbursement Request`)

### 2. 列表页 (List View) 胶囊徽章与防折行规范
- **独立卡片质感 (Badges)**：每个品名与数量包装为独立小卡片，灰白背景（`#f8fafc`）、浅边框（`#cbd5e1`）、微阴影，数量/金额以宝蓝内嵌徽章高亮显示。
- **绝对单行不折行 (No-Wrap)**：列表行必须保持单行（`flex-wrap: nowrap; overflow: hidden;`），**严禁因多卡片折行撑高表格行高度**，确保整页行高均匀一致。
- **多项优雅省略卡片 (`+N···`)**：单行默认展示前 2 个完整卡片，超出部分自动收敛为微型卡片（如 `[ +1··· ]`、`[ +3··· ]`）。
- **悬浮 Tooltip 全量预览**：鼠标悬停在卡片或省略号上时，自动触发浏览器原生 `title` 浮窗，秒级查看该单据的全部物料及明细信息。

---

## 📅 五、我的定期任务 (月度报表核定待办) 架构与多主体隔离标准

企业运营具有明显的**月度周期性核定特征**。系统在总控主页（`Home` / `My Business`）设立 **【我的定期任务 (月度报表核定)】** 中心，实现定期核定事项的智能推导与权限自适应呈现。

### 1. 多主体独立卡片划分
核定事项按归属公司划分为两张独立的月度报表核定卡片：
1. 🏢 **【吉众 · YYYY年MM月报表核定】** (天津吉众机电设备有限公司)
   - ⛽ **油卡明细核定**：统计该月加油记录与油卡台账，未核定显示 `⚠️ 未核定 · 立即核定 ➔`，已核定显示 `✅ 已核定`。
   - 🛣️ **车辆高速费核定**：根据吉众名下车辆及当月通行流水动态判定；**若吉众当月无高速费车辆/记录则自动不显示**；有记录且已提交显示 `✅ 已核定`，未完成显示 `⚠️ 未核定`。
2. 🏭 **【祺富 · YYYY年MM月报表核定】** (天津祺富机械加工有限公司)
   - 💡 **水电费月结**：关联水电费月结工作台（抄表、倍率换算、用电调整与分摊），已过账显示 `✅ 已核定`，未完成显示 `⚠️ 未核定 · 立即核定 ➔`。
   - 🏠 **房租与物业费月结**：关联房租与物业费工作台（930㎡仓库等租赁与税率核算），已结账显示 `✅ 已核定`，未完成显示 `⚠️ 未核定 · 立即核定 ➔`。

### 2. 角色与权限智能感知 (Adaptive UX)
- **油卡操作员 / 油卡管理员**：仅显示其所属公司（如吉众）的油卡明细与车辆高速费核定任务。
- **物业管理员 / 操作员 / 财务**：显示水电费、房租物业费月结核定任务。
- **系统管理员 (Administrator / System Manager)**：拥有全局视野，吉众与祺富两大主体的月度报表核定卡片并排全量展示。
- **无对应权限普通员工**：定期任务卡片自动隐藏，保持界面纯净低噪。

### 3. 时间推导与闭环逻辑
- 默认核定周期为**上一自然月**（如当前为 9 月 1 日，自动核定 8 月报表；当前为 8 月中旬，自动核定 7 月报表），支持在控制栏直接切换月份。
- 点击 `⚠️ 未核定 · 立即核定 ➔` 可一键 SPA 路由无缝直达对应工作台或单据大屏，完成核定并提交后，主页状态即时自动切换为 `✅ 已核定`。

---

## 🛠️ 六、测试与交付标准

1. **修改流程严格遵循标准工作流**：
   - 本地修改 -> `sync_app.py` 安全直传容器 -> 自动化/API 测试 -> 用户确认后再推送 GitHub。
2. **自动化测试覆盖**：
   - 任何涉及金额、计费公式、Excel 导出、定期任务推导的修改，必须包含单元测试，保证 100% 通过。
3. **多环境安全原则**：
   - 严禁在脚本中硬编码密码与 Token，必须自动加载 `.env`。
